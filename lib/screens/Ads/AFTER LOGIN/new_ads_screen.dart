import 'package:flutter/material.dart';

class NewAdsScreen extends StatelessWidget {
  const NewAdsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("NEWADSSGEEN")),
    );
  }
}
